// Define the type for your initial state
    export interface FileState {
        document: string[]
        file: {
            document: any[];
        };
    }
    export interface RootState {
            document: any[]
    }